{
  "name": "maxxed-technical-systems-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate-site.mjs && node scripts/validate-artifact.mjs",
    "check:public": "npm run build && npm run validate",
    "check:admin": "npm --prefix platform run check",
    "check": "npm run check:public && npm run check:admin"
  }
}
