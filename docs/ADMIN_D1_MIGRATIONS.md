# Admin D1 Migrations

The admin platform uses a migration catalog under admin/db/migrations/.

Run: `npm run admin:d1:check`

Apply migrations in ascending filename order. Do not store D1 IDs, tokens, or account secrets in source.
